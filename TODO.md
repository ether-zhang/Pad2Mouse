# TODO

后续待办，按需排期。

- [x] 支持开机自启控制，放入设置（注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 项）
- [x] 更换图标（当前是占位 P2M 字形）
- [ ] 主页 / 设置换成更易懂的名称
- [x] 支持 Xbox 手柄（XInput 或 HID 枚举不同 VID/PID）
- [x] 支持多设备连接
- [x] 去掉实时数据面板，腾出空间展示按键映射
- [x] 支持运行时更换按键映射（当前硬编码在 `MapperEngine`）
- [ ] GUI 优化，更直观的图片映射，更现代的界面
- [x] 不用主机徽键, 而是 L3+R3 控制开启与关闭
- [x] 增加键盘映射（下拉「按键…」唤起内置键盘，点选任一键即设为按住式映射，存为 `Key:0xNN`）
- [x] 组合键打开虚拟键盘（L1+R1 切换内置半透明 QWERTY 键盘）

GUI更新
- [x] 界面变得更加现代化，虚拟键盘那种风格就比较合适
- [x] 根据连接的手柄切换不同主题色，PS手柄淡蓝，Xbox手柄浅绿，如果两个都连接了，就蓝绿渐变
- [x] 当有手柄连接时，更改为显示 DualSense Edge(蓝牙) 这样更直观且适合多设备的
- [x] ico图标放大20%
- [ ] 按键映射处的空间有点浪费，换成展示一个手柄图片，然后按键处周围放对应的映射下拉栏

遗留 bug
- [ ] BT + USB 同一只 DualSense 同时在线时，连接显示偶尔仍会出现两条；现有按 HID 序列号去重的逻辑在某些时序下没生效 — 需复现并定位（可能是 `GetSerialNumber()` 在某种连接下返回 null，或两条枚举几乎同时出现导致 supersede 判断错过）